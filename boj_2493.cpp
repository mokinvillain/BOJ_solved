#include <bits/stdc++.h>

using namespace std;

typedef long long ll;
typedef pair<ll,ll> pll;

vector<ll> s;
ll ret[501010],N,pt;
string cmd;

int main(){
    cin.tie(0);
    ios::sync_with_stdio(0);
    for(int i = 0;i<N;i++){
        cin >> cmd;
    }
}